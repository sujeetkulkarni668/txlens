# TxLens API — core dependencies (Phase 1 scaffold).
# STATUS: not installed/verified in this environment (no network access in
# the build sandbox). Install locally with:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2
redis==5.0.8
python-multipart==0.0.9
slowapi==0.1.9
httpx==0.27.2
structlog==24.4.0

# Note: no python-jose/passlib — authentication (app/auth/) uses a
# stdlib-only PBKDF2 password hasher and a minimal HS256 JWT
# implementation instead, specifically so that code could be genuinely
# unit-tested in an environment with no network access to install
# anything. See app/auth/password.py and app/auth/jwt.py.
